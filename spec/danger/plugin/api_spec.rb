require 'spec_helper'

describe Danger::Plugin::Api do
  it 'has a version number' do
    expect(Danger::Plugin::Api::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
